{
  "name": "peckboard-playwright-video-plugin",
  "version": "0.3.3",
  "private": true,
  "description": "Peckboard playwright-video WASM plugin (Extism js-pdk / TypeScript) — LogRocket-style replay of recorded browser test runs",
  "scripts": {
    "bundle": "node esbuild.js",
    "build": "node esbuild.js && extism-js dist/index.js -i src/index.d.ts -o dist/plugin.wasm",
    "test": "vitest run"
  },
  "devDependencies": {
    "@extism/js-pdk": "^1.1.1",
    "esbuild": "^0.23.1",
    "vitest": "^2.1.9"
  }
}
